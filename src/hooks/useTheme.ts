import {useEffect} from 'react';import type {Preferences} from '../domain/types';
export function useTheme(p:Preferences){useEffect(()=>{const media=window.matchMedia('(prefers-color-scheme: dark)');function apply(){document.documentElement.dataset.theme=p.theme==='system'?(media.matches?'dark':'light'):p.theme;document.documentElement.style.setProperty('--font-scale',String(p.fontScale));document.documentElement.dataset.density=p.density;document.documentElement.style.setProperty('--card-columns',String(p.columns));if(p.theme!=='dark'&&p.theme!=='eink'&&!media.matches)document.documentElement.style.setProperty('--accent',p.accent);else document.documentElement.style.removeProperty('--accent')}apply();media.addEventListener('change',apply);return()=>media.removeEventListener('change',apply)},[p.theme,p.fontScale,p.density,p.columns,p.accent])}
