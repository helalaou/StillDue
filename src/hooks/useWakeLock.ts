import {useEffect,useState} from 'react';
export function useWakeLock(enabled:boolean){const [status,setStatus]=useState('');useEffect(()=>{let sentinel:WakeLockSentinel|null=null;let disposed=false;async function request(){if(!enabled||document.visibilityState!=='visible')return;if(!('wakeLock' in navigator)){setStatus('Keep-awake is not supported by this browser.');return}try{const lock=await navigator.wakeLock.request('screen');if(disposed){await lock.release();return}sentinel=lock;setStatus('Screen will stay awake while this page is visible.')}catch{setStatus('Your device could not keep the screen awake.')}}void request();document.addEventListener('visibilitychange',request);return()=>{disposed=true;document.removeEventListener('visibilitychange',request);void sentinel?.release();setStatus('')}},[enabled]);return status}
