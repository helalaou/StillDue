import {useServiceStatus} from '../hooks/useServiceStatus';
import {useState,type FormEvent} from 'react';import {ArrowRight,Mail,Eye,EyeOff} from 'lucide-react';import {Brand} from '../components/Brand';import {useAuth} from '../context/AuthContext';import {supabase} from '../lib/supabase';import {errorMessage} from '../lib/errors';
export function AuthPage(){const auth=useAuth();const service=useServiceStatus();const [mode,setMode]=useState<'login'|'signup'|'reset'>( 'login'),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[name,setName]=useState(''),[show,setShow]=useState(false),[busy,setBusy]=useState(false),[message,setMessage]=useState(''),[error,setError]=useState('');
async function submit(e:FormEvent){e.preventDefault();setError('');setMessage('');if(!supabase){setError('Cloud accounts are not configured in this installation. You can explore the demo.');return}if(!auth.recovery&&mode!=='login'&&!service.data?.authEmail){setError('Email delivery is being set up. Please explore the demo for now, or sign in to an existing confirmed account.');return}setBusy(true);try{if(auth.recovery){const {error}=await supabase.auth.updateUser({password});if(error)throw error;auth.finishRecovery();return}if(mode==='signup'){const {error}=await supabase.auth.signUp({email,password,options:{data:{name},emailRedirectTo:location.origin+'/auth'}});if(error)throw error;setMessage('Check your email to confirm your account, then come back to sign in.')}else if(mode==='reset'){const {error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:location.origin+'/auth'});if(error)throw error;setMessage('If an account exists for this email, a reset link is on its way.')}else{const {error}=await supabase.auth.signInWithPassword({email,password});if(error)throw error}}catch(e){setError(errorMessage(e))}finally{setBusy(false)}}
return <div className="auth-page"><section className="auth-story"><Brand/><div className="auth-story-content"><span className="eyebrow">KEEP IT IN VIEW</span><h1>A little clarity.<br/>A little more<br/><em>headspace.</em></h1><p>A calm home for your deadlines, your research, and whatever comes next.</p><div className="auth-example"><span>YOUR NEXT SMALL STEP</span><p>Make room for the work that matters.</p><div className="auth-track"><i/></div><small>Your pace. Your space.</small></div></div><small>Made for screens you live with.</small></section><section className="auth-panel"><div className="auth-form-wrap"><span className="eyebrow">WELCOME TO STILLDUE</span><h2>{auth.recovery?'Choose a new password':mode==='signup'?'Make yourself at home.':mode==='reset'?'Let’s get you back in.':'Good to see you.'}</h2><p className="muted">{mode==='signup'?'Your deadlines, in one quiet place.':mode==='reset'?'We’ll send a link to reset your password.':'Sign in to pick up where you left off.'}</p>{mode!=='login'&&!auth.recovery&&!service.data?.authEmail&&<p className="email-pending">Email delivery is being set up. New account verification and password resets will be available once it is connected.</p>}<form onSubmit={submit}>{mode==='signup'&&!auth.recovery&&<label className="field">Your name<input autoComplete="name" maxLength={100} required value={name} onChange={e=>setName(e.target.value)}/></label>}{!auth.recovery&&<label className="field">Email address<input type="email" autoComplete="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></label>}{(mode!=='reset'||auth.recovery)&&<label className="field">Password<span className="password-input"><input type={show?'text':'password'} autoComplete={mode==='signup'||auth.recovery?'new-password':'current-password'} minLength={mode==='signup'||auth.recovery?12:1} required value={password} onChange={e=>setPassword(e.target.value)}/><button type="button" className="icon-button" aria-label={show?'Hide password':'Show password'} onClick={()=>setShow(!show)}>{show?<EyeOff size={18}/>:<Eye size={18}/>}</button></span>{(mode==='signup'||auth.recovery)&&<small>Use at least 12 characters.</small>}</label>}{error&&<p className="form-error" role="alert">{error}</p>}{message&&<p className="form-success" role="status"><Mail size={18}/>{message}</p>}<button className="button primary full" disabled={busy}>{busy?'One moment…':auth.recovery?'Save password':mode==='signup'?'Create account':mode==='reset'?'Send reset link':'Sign in'}<ArrowRight size={18}/></button></form>{!auth.recovery&&<><div className="auth-links"><button className="text-button" onClick={()=>{setMode(mode==='signup'?'login':'signup');setMessage('');setError('')}}>{mode==='signup'?'Already have an account? Sign in':'New here? Create an account'}</button>{mode==='login'&&<button className="text-button" onClick={()=>setMode('reset')}>Forgot password?</button>}{mode==='reset'&&<button className="text-button" onClick={()=>setMode('login')}>Back to sign in</button>}</div><div className="auth-divider"><span>Take a look around first</span></div><button className="button secondary full" onClick={auth.enterDemo}>Explore the demo <ArrowRight size={17}/></button><p className="fine-print">The demo uses fictional data stored only in this browser.</p></>}</div></section></div>}
