import {useState} from 'react';import {useNavigate,useSearchParams,Link} from 'react-router-dom';import {Plus,Search,LayoutGrid,List,Monitor,ArrowUpRight,SlidersHorizontal,Bookmark,Trash2,Check} from 'lucide-react';import {useWorkspace} from '../context/WorkspaceContext';import {useToast} from '../context/ToastContext';import {DeadlineCard} from '../components/DeadlineCard';import {DeadlineEditor} from '../components/DeadlineEditor';import {DeadlineDetail} from '../components/DeadlineDetail';import {EmptyState} from '../components/EmptyState';import {ConfirmDialog} from '../components/ConfirmDialog';import {newDeadline} from '../domain/defaults';import {filterDeadlines} from '../domain/filter';import {complete,trash,effectiveStatus} from '../domain/lifecycle';import {useClock} from '../hooks/useClock';import type {Deadline} from '../domain/types';import {formatDate} from '../domain/time';
export function OverviewPage({status='active',focus=false}:{status?:string;focus?:boolean}){const {data,save,remove,setPreferences,saving}=useWorkspace(),toast=useToast(),navigate=useNavigate();const [params,setParams]=useSearchParams();const search=params.get('q')||'',boardId=params.get('board')||'',priority=params.get('priority')||'',sort=params.get('sort')||'due';const [view,setView]=useState<'grid'|'list'>('grid'),[editor,setEditor]=useState<Deadline|null>(null),[detail,setDetail]=useState<Deadline|null>(null),[selection,setSelection]=useState<string[]>([]),[selecting,setSelecting]=useState(false),[emptyTrash,setEmptyTrash]=useState(false);const now=useClock(30),prefs=data.preferences;const active=data.deadlines.filter(d=>effectiveStatus(d,prefs,now)==='active');const filtered=filterDeadlines(data.deadlines,prefs,{status,search,boardId,priority,sort,focus},now);const items=focus?filtered.slice(0,prefs.focusLimit):filtered;function filter(key:string,value:string){setParams(p=>{const n=new URLSearchParams(p);value?n.set(key,value):n.delete(key);return n})}const title=focus?'One thing at a time.':status==='inactive'?'A little breathing room.':status==='completed'?'Look what you’ve done.':status==='trash'?'Nothing lost by accident.':'Room to focus.';const desc=focus?'Your chosen priorities. Start with one small step.':status==='inactive'?'Past deadlines and things you’ve set aside.':status==='completed'?'A record of the things you finished.':status==='trash'?'Restore what you need. Permanently delete only when you’re ready.':'Everything coming up. A little more headspace.';
async function bulk(action:'complete'|'trash'){let count=0;for(const d of data.deadlines.filter(d=>selection.includes(d.id))){if(await save('deadlines',action==='complete'?complete(d):trash(d)))count++}setSelection([]);setSelecting(false);toast(`${count} deadlines ${action==='complete'?'completed':'moved to Trash'}.`)}
return <div className="page"><div className="page-heading"><div><div className="eyebrow">{focus?'A SMALLER VIEW':status==='active'?'A LITTLE CLARITY, EVERY DAY':status.toUpperCase()}</div><h1>{title}</h1><p>{desc}</p></div>{status==='trash'?<button className="button secondary danger-text" disabled={!items.length} onClick={()=>setEmptyTrash(true)}><Trash2 size={17}/>Empty trash</button>:<button className="button primary" onClick={()=>setEditor(newDeadline(boardId||data.boards[0]?.id||null))}><Plus size={19}/>New deadline</button>}</div>{status==='active'&&!focus&&<div className="overview-strip"><div><span className="stat-value">{String(active.length).padStart(2,'0')}</span><span>active deadlines</span></div><div><span className="stat-value">{String(active.filter(d=>d.dueAt&&Date.parse(d.dueAt)>now&&Date.parse(d.dueAt)-now<7*86400000).length).padStart(2,'0')}</span><span>due this week</span></div><div><span className="stat-value">{String(active.filter(d=>d.focus).length).padStart(2,'0')}</span><span>kept in focus</span></div><div className="strip-note">Make the next step<br/><strong>a small one.</strong><ArrowUpRight size={22}/></div></div>}<div className="section-toolbar"><h2>{focus?'In focus':status==='active'?'In view':status.charAt(0).toUpperCase()+status.slice(1)} <span className="count">{filtered.length}</span></h2><div className="view-switch" aria-label="View style"><button className={view==='grid'?'active':''} aria-label="Card view" onClick={()=>setView('grid')}><LayoutGrid size={17}/></button><button className={view==='list'?'active':''} aria-label="List view" onClick={()=>setView('list')}><List size={18}/></button></div></div><div className="filter-bar"><div className="search-input"><Search size={17}/><input aria-label="Search deadlines" placeholder="Find a deadline…" value={search} onChange={e=>filter('q',e.target.value)}/></div><select aria-label="Filter by board" value={boardId} onChange={e=>filter('board',e.target.value)}><option value="">All boards</option>{data.boards.map(b=><option key={b.id} value={b.id}>{b.name}</option>)}</select><select aria-label="Filter by priority" value={priority} onChange={e=>filter('priority',e.target.value)}><option value="">Any priority</option><option value="high">High priority</option><option value="normal">Normal</option><option value="low">Low</option></select><select aria-label="Sort deadlines" value={sort} onChange={e=>filter('sort',e.target.value)}><option value="due">Soonest first</option><option value="priority">Priority first</option><option value="title">By title</option></select><button className="icon-button" title="Save this view" aria-label="Save this view" onClick={async()=>{const name=window.prompt('Name this view');if(name?.trim()&&await setPreferences({...prefs,savedViews:[...prefs.savedViews,{name:name.trim(),boardId,search,priority}]}))toast('View saved.')}}><Bookmark size={18}/></button><button className={'icon-button '+(selecting?'selected':'')} aria-label="Select multiple deadlines" onClick={()=>{setSelecting(!selecting);setSelection([])}}><SlidersHorizontal size={18}/></button></div>{prefs.savedViews.length>0&&<div className="saved-views">{prefs.savedViews.map((v,i)=><button key={i} onClick={()=>setParams({board:v.boardId,q:v.search,priority:v.priority})}><Bookmark size={12}/>{v.name}</button>)}</div>}{selecting&&<div className="bulk-bar"><span>{selection.length} selected</span><button className="text-button" onClick={()=>setSelection(items.map(d=>d.id))}>Select all visible</button>{status!=='trash'&&<><button className="button secondary" disabled={!selection.length||saving} onClick={()=>void bulk('complete')}><Check size={15}/>Complete</button><button className="button secondary" disabled={!selection.length||saving} onClick={()=>void bulk('trash')}><Trash2 size={15}/>Move to Trash</button></>}</div>}{items.length?view==='grid'?<div className="deadline-grid">{items.map(d=><DeadlineCard key={d.id} item={d} board={data.boards.find(b=>b.id===d.boardId)} project={data.projects.find(p=>p.id===d.projectId)} prefs={prefs} now={now} onOpen={()=>setDetail(d)} onComplete={async()=>{if(await save('deadlines',complete(d)))toast('Marked complete.')}} selectable={selecting} selected={selection.includes(d.id)} onSelect={()=>setSelection(s=>s.includes(d.id)?s.filter(id=>id!==d.id):[...s,d.id])}/>)}</div>:<div className="deadline-list">{items.map(d=><div key={d.id} className="deadline-list-row">{selecting&&<input type="checkbox" aria-label={'Select '+d.title} checked={selection.includes(d.id)} onChange={()=>setSelection(s=>s.includes(d.id)?s.filter(id=>id!==d.id):[...s,d.id])}/>}<button onClick={()=>setDetail(d)}><strong>{d.title}</strong><span>{data.boards.find(b=>b.id===d.boardId)?.name||d.kind} · {d.nextAction||'No next step yet'}</span></button><span>{formatDate(d.dueAt,prefs.followDevice?Intl.DateTimeFormat().resolvedOptions().timeZone:prefs.timezone,prefs.hour24,true)}</span></div>)}</div>:<EmptyState title={focus?'Choose a little less to focus on.':search||boardId||priority?'Nothing matches this view.':status==='active'?'Start with one thing.':status==='trash'?'Trash is empty.':status==='completed'?'Your finished work will live here.':'Nothing set aside.'} description={focus?'Open a deadline and choose “Keep in focus” to bring it here.':search||boardId||priority?'Try another search or clear your filters.':status==='active'?'Add a deadline, start from a template, or discover a conference.':'You can always return here when you need to.'} action={status==='active'&&!focus?<div className="inline-actions"><button className="button primary" onClick={()=>setEditor(newDeadline(data.boards[0]?.id||null))}><Plus size={17}/>Add a deadline</button><Link className="button secondary" to="/templates">Explore templates</Link></div>:undefined}/>} {focus&&filtered.length>items.length&&<p className="muted">Showing your first {prefs.focusLimit} priorities. Change the focus limit in Settings.</p>}{status==='active'&&!focus&&<div className="quiet-banner"><div><span className="eyebrow">YOUR SPACE, YOUR PACE</span><h3>A board that belongs on your desk.</h3><p>Open display mode and keep what matters in view.</p></div><button className="button secondary" onClick={()=>navigate('/display')}><Monitor size={18}/>Open display</button></div>}{editor&&<DeadlineEditor key={editor.id} item={editor} onClose={()=>setEditor(null)}/>} {detail&&!editor&&<DeadlineDetail item={detail} onClose={()=>setDetail(null)} onEdit={d=>{setDetail(null);setEditor(d)}}/>}{emptyTrash&&<ConfirmDialog title="Empty Trash permanently?" message="Every deadline in Trash will be permanently removed, including items hidden by your current filters. This cannot be undone." label="Empty Trash" onClose={()=>setEmptyTrash(false)} onConfirm={async()=>{for(const d of data.deadlines.filter(d=>d.status==='trash'))await remove('deadlines',d)}}/>}</div>}
