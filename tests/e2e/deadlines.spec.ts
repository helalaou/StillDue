import {test,expect} from '@playwright/test';import {enterDemo,navigate} from './helpers';
test('create, edit, complete and restore a deadline',async({page})=>{await enterDemo(page);await page.getByRole('button',{name:'New deadline',exact:true}).click();await page.getByLabel('Title',{exact:true}).fill('Browser test deadline');await page.getByRole('tab',{name:'Next steps'}).click();await page.getByLabel('Your next small step').fill('Write one paragraph');await page.getByRole('button',{name:'Add deadline',exact:true}).click();await expect(page.getByRole('button',{name:'Browser test deadline',exact:true})).toBeVisible();await page.getByRole('button',{name:'Open Browser test deadline',exact:true}).click();await expect(page.getByText('Write one paragraph',{exact:true}).last()).toBeVisible();await page.getByRole('button',{name:'Move to Trash',exact:true}).click();await navigate(page,'Trash');await page.getByRole('button',{name:'Open Browser test deadline'}).click();await page.getByRole('button',{name:'Restore',exact:true}).click();await navigate(page,'Overview');await page.getByRole('button',{name:'Open Browser test deadline'}).click();await page.getByRole('button',{name:'Mark done',exact:true}).last().click();await navigate(page,'Completed');await expect(page.getByRole('button',{name:'Browser test deadline',exact:true})).toBeVisible()});
test('unfinished new drafts survive closing the editor',async({page})=>{await enterDemo(page);await page.getByRole('button',{name:'New deadline',exact:true}).click();await page.getByLabel('Title',{exact:true}).fill('A draft to recover');await page.getByRole('button',{name:'Close dialog'}).click();await page.getByRole('button',{name:'New deadline',exact:true}).click();await expect(page.getByLabel('Title',{exact:true})).toHaveValue('A draft to recover')});
